#cloud-config
users:
  - name: jdelpino
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ${ssh_key}
    shell: /bin/bash
ssh_pwauth: false
disable_root: true
growpart:
  mode: auto
  devices: ['/']
